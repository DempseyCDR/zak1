# Backlog — Deferred & Cross-Cutting Items

Items intentionally out of scope for their originating feature, captured so they aren't lost. Each
notes where it surfaced and where it will likely land. Not a commitment to a phase.

| # | Item | Surfaced in | Likely home | Notes |
|---|------|-------------|-------------|-------|
| B1 | **Group tickets** — a single ticket purchased once and redeemable as admission across all events in an `EventGroup` (Double Dance, weekend festival, JAB). | 002 (EventGroup entity added; tickets deferred) | 007 (online purchase) + 002 (door redemption) + 004/005 (revenue attribution across constituent events) | Entity `event_group` + `event.group_id` exist as of feature 002. Needs: ticket purchase, redemption-at-door, and splitting one ticket's revenue across events. |
| B2 | **Non-volunteer contact login + self-service profiles** | 001 (Phase 1 = volunteer/admin login only) | Future auth phase | Deferred per source doc. |
| B3 | **Primary email designation** | 001 (no primary email in Phase 1) | Future | Needed if a single canonical public-profile address must be chosen among several. |
| B4 | **Cross-club shared performer directory** | source doc / 003 | Future / multi-tenant | Deferred. |
| B5 | **Configurable fiscal quarters** | 005 (calendar quarters assumed) | Future | Organizer report currently uses calendar quarters. |
| B6 | **Separate Venmo fee rate** | source doc / 004 | Future | Phase 1 shares the PayPal/Venmo door fee; admin splits when rates diverge. |
| B7 | **iContact API sync** | 006 (CSV export only in Phase 1) | Future | Platform is system of record; CSV is the Phase 1 delivery path. |
| B8 | **QBO API integration** | 004 (manual copy/paste Treasurer Report in Phase 1) | Future | No CSV import / API in Phase 1. |
| B9 | **Native iOS/Android apps** | 007 | Future | Web only for now. |
| B10 | **Automated email from the platform** | 006 / source doc | Future | Platform produces lists; sending stays in iContact. |
| B11 | **Multi-tenant data model** | build 1 chosen single-tenant | Future build/refactor | Build 1 is single-tenant (CDR); club-level settings already configurable. |
| B12 | **Additional event attributes (venue, etc.)** — *RETIRED → P2-4* | 002/003 (event creation added) | **Retired → Phase 2 (P2-4)** | **Retired.** Venue half already delivered by feature 007 (venues + maps + `events.venue_id`). Remaining event attributes — short `label`, `start_time`, `description` — are specified in `specs/PHASE2_REQUIREMENTS.md` **P2-4**. No further backlog action. |
| B14 | **Comps (free admissions) at the door** — *RETIRED → P2-5* | 005 clarify (paying-dancers derivation) | **Retired → Phase 2 (P2-5)** | **Retired, superseded by `specs/PHASE2_REQUIREMENTS.md` P2-5.** Add `comp_count` to `door_records`; `paying_dancers = attendance − performers − door attendant − comps`. Gift-card redeemers stay counted as paying (no change to `gift_card_redemption_count`). No further backlog action. |
| B15 | **Reusable Band roster entity** — *DONE (feature 008)* | 003 (musician type added) | **Done → feature 008** | **Implemented as feature 008 (`specs/008-band-roster`), committed `7159668`.** `bands` schema + `/bands` UI + band-booking API (`/api/bands`, `/api/events/[id]/book-band`); a Band (lead + member musicians) is reusable and bookable as a unit. |
| B16 | **Consolidate `rateParameters` + `SeriesExpenseParameter` into one `series_parameters` entity** — *DONE (feature 009)* | 003 (rate params) / 005 (expense params) | **Done → feature 009** | **Implemented as feature 009 (`specs/009-series-parameters`), committed `f75d0f7`.** One `series_parameters` table (`category` rate/expense, `kind`, **NOT NULL** `series_id`, `amount_cents`, `label`, `effective_date`) + shared `series_parameter_audit`; added a `general` series and a `musician` rate kind; one `resolveParameterCents` resolver replaced `resolveRateCents`/`resolveExpenseCents`; migration `0012` backfilled global rates into every series so no resolved figure changed. **Note:** P2-2 (B19) revisits this — rent re-scopes to venue + performer-pay family, ongoing becomes a recurring series charge. |
| B17 | **Incremental mailing-list export** — download only the addresses that changed since a list's last export, instead of the full qualifying set every time. | 006 (plan review) | 006 domain/exports | Each of the 7 lists already gets its own on-demand download (FR-010) and every export writes a `mailing_list_exports` row with `created_at` (B16-adjacent audit design). An incremental mode would compare `contact_emails.updated_at` (and/or `contacts.updated_at`) against that list's most recent `mailing_list_exports.created_at` and export only rows that changed since. Deferred: Phase 1 always exports the full set; needs a decision on what counts as "changed" (email row edits vs. consent-topic changes vs. contact display-name edits) before implementing. |
| B18 | **Self-service "create dance series"** — an admin UI + `POST /api/series` to add a new series (e.g. "Squares") without operator/DB access. | data-model review 2026-07-04 | 002 events + admin config | Series is a **table**, not an enum, so adding one needs no schema/migration/build — but there's no write path today (`/api/series` is GET-only; series were only ever seeded). Small addition: `seriesCreateSchema` (key/name/has_sound_tech), a `createSeries` service + audit, `POST /api/series`, and a create-series form (fold into events/admin config). Note the new series then needs its own config rows to be fully functional: rate/expense parameters (self-service via existing pages; no cross-series fallback — resolves to $0 until set) and a `series_qbo_map` row for the treasurer report (endpoint exists, no UI). Deferred: new-series creation is rare. |
| B19 | **Reshape rate/expense parameters (revisit 009)** — move rent in with performer pay (venue-scoped standard + per-event override), keep ongoing as a recurring series charge ended by a $0 effective-dated row, retire the artificial `kind`/`label` split. | data-model review 2026-07-06 | Phase 2 (**P2-2**) | Promoted to Phase 2: see `specs/PHASE2_REQUIREMENTS.md` **P2-2** for the full write-up (context, what-must-change, testable outcomes, clarify questions). Refines — does not extend — feature 009 / B16: 009 merged rate+expense on a shape coincidence, but by behavior there are three patterns (standard-with-per-instance-override keyed by a dimension = performer pay + rent; recurring auto-applied series charge = ongoing; per-event ad-hoc = misc). Depends on `venues` + `events.venue_id` (feature 007, built). |
| B20 | **Contact first/last name, overridable display name, pronouns** | data-model review 2026-07-06 | Phase 2 (**P2-3**) | Promoted to Phase 2: see `specs/PHASE2_REQUIREMENTS.md` **P2-3**. Add required `first_name`/`last_name` (check-in sort + separate mailing-list columns), a nullable `display_name_override` (effective display name = `override ?? "first last"`; the member-button label), and free-text `pronouns`. No backfill — contacts load fresh from the club's existing lists at go-live. Touches features 001 (contacts/dedup/UI), 002 (check-in), 006 (export columns). |
| B21 | **Dormant `gift_card_redemption_count`** — *direction decided → surface, captured at check-in (see B29)* | 014 / P2-5 review 2026-07-08; revisited 2026-07-12 (Door Attendant/FS) | Phase 3 (user roles + UI) → **B29** | `door_records.gift_card_redemption_count` (a *count* of redemptions) has existed since feature 002 (`0004_door.sql`) but is **orphaned**: in the schema, `doorRecordPatchSchema`, and `updateDoorRecord`, yet **no UI sets it** and **no report reads it** (only tests). Distinct from the `gift_card` gate-*sales* dollar line, which *is* on the gate page. **2026-07-12 decision:** keep the column and **surface it — captured at check-in by the Door Attendant, materialized on the door record, confirmed by the FS** (see **B29**), not dropped. Still counts-only (no attendee attribution); gift-card redeemers stay counted as paying (P2-5 unchanged). |
| B22 | **Venue landlord contact** — link a venue to the `contact` who is its landlord (the party the Booker negotiates rent with). | Phase 3 roles/use-case review 2026-07-12 (Booker) | Phase 3 (roles + UI) / `venues` | Surfaced clarifying the Booker role: it is the Booker who negotiates rent with the venue landlord, so a venue needs an associated landlord contact. Add a nullable `landlord_contact_id` FK on `venues` → `contacts` (a contact, reusing the existing directory rather than free-text). Enables surfacing landlord name/contact info on the venue admin page and, later, rent-negotiation context. Decide: single landlord vs. multiple contacts per venue; whether the landlord is flagged/roled as such on the contact. |
| B23 | **Booking status lifecycle** — per-booking status: **proposed → requested → confirmed**, plus **declined**. | Phase 3 roles/use-case review 2026-07-12 (Booker) | Phase 3 (roles + UI) / `bookings` | The Booker *requests* a performer for a date (`requested`); on a decline the Booker either re-points the same booking slot to another performer (back to `proposed`) or parks the slot (`declined`) to defer proposing a replacement; `confirmed` once the performer agrees to the date **and** the pay rate. Status is **per booking**. Needs a `booking_status` enum + column (default `proposed`), transitions in `bookingService` + `bookBand`, and status controls on `/bookings`. Existing `bookings.note` optionally records context ("Katy said no"). ⚠️ Swapping the performer on the same row loses structured decline history — only the optional note preserves it; accepted by the Booker. `bookings` already has `requiresCheck`/`checkNumber`/`isOverridden`/`isDonated` booleans but **no status** today. |
| B24 | **Booker bookings report (cross-event)** — a dynamic report of bookings across past & future events, filterable, for talent-distribution planning. | Phase 3 roles/use-case review 2026-07-12 (Booker) | Phase 3 (roles + UI) / new `domain/bookings` report | Lists bookings across events — **date · caller · band (if named) · musicians · sound tech** — so the Booker can assure appropriate bookings and a fair distribution of performer talent across a series (e.g. "show me all the dances using Bob Fabinski"). **Filters required:** caller, band, individual musician, **series**, and **date range**. A new *read-across-events* view (unlike the per-event treasurer/organizer reports). Likely depends on B23 if the report should distinguish proposed/requested/confirmed bookings. No such report or endpoint exists today. |
| B25 | **Event lifecycle: cancel vs. delete, plus reschedule** — Booker-owned. | Phase 3 roles/use-case review 2026-07-12 (Booker) | Phase 3 (roles + UI) / `events` | The Booker cancels, deletes, reschedules, and edits events. **Cancel** = a retained state, kept in the record **and shown on the public website** (so the public knows not to come) → needs an event `cancelled` status/flag surfaced on `/whats-on`. **Delete** = hard removal (assumed a mistake). **Reschedule** = change `event_date`, which the current event PATCH does **not** allow (`assignVenueSchema` covers venue/rent/label/startTime/description only). Today `/api/events/[id]` is GET+PATCH (no DELETE), and `events` has no status column. Needs: `cancelled` state, a DELETE path, and `eventDate` added to the event PATCH. |
| B26 | **Recurring event generation** — define first date + increment + last date to bulk-create many independent event rows. | Phase 3 roles/use-case review 2026-07-12 (Booker) | Phase 3 (roles + UI) / `events` | The Booker defines a repeating event (**first date, increment, last date**) that generates many rows in `events`; each generated event is then independently editable/cancellable **without affecting the others** (rows are independent, not a live recurrence rule). Needed because **tnc** dances every Thursday and **ecd** every Sunday (winter afternoons vs. summer evenings — the seasonal start-time difference is handled by running the generator twice with different `start_time`, since rows are independent). No bulk/recurrence create exists today (events are created one at a time). |
| B27 | **Advertised admission price (Webmaster-owned, public)** | Phase 3 roles/use-case review 2026-07-12 (Booker/Webmaster) | Phase 3 (roles + UI) / `events` + public site | The **Webmaster** maintains the admission price shown **on the website**. `events` today has only a `charges_admission` boolean — no price field. Needs a public-facing admission-price field on the event, editable by the Webmaster and displayed on `/whats-on`. Note: door-level price **overrides** at the point of sale are handled by the credit-card POS device and are **out of scope**; and gate/admission revenue is still *derived* from the door record (this price is display-only, not an accounting input). |
| B30 | **Online membership purchase (PayPal buy button)** — self-service dues payment on the website. | Phase 3 roles/use-case review 2026-07-12 (membership acquisition) | Phase 3 / public site + `memberships` (relates to 007 US2 / B1) | A contact becomes a member by paying dues via a **PayPal buy button on the website**; the info required to update the contact's membership status is captured on the website, and payment is confirmed by the **existing PayPal→QBO feed** (reconciliation in QBO, out of scope). This is the first *online sale* to actually land — narrower than full advance-ticket sales (**B1 / 007 US2**, still deferred): only membership, one buy button, no group tickets. Needs a public purchase flow + a way to create/renew the membership record from the captured info. |
| B31 | **Door membership enrollment — link gate `membership` payment to a membership record** | Phase 3 roles/use-case review 2026-07-12 (membership acquisition) | Phase 3 / `domain/gate` + `domain/membership` | A contact may pay dues at the door in **cash** (to the Door Attendant) or by **card/Venmo**; the **FS enters the contact's name on `/gate`**. Today the gate `membership` category is a **dollars-only** line (`eventMoney.membershipCents`) with an optional `contactId` — it does **not** call `createMembership`, so no membership record/status change results. Needs: entering a named door membership payment creates/renews the `memberships` record and recomputes contact status (`membershipService.createMembership` + `recomputeContactStatus`), atomically with the gate sale. |
| B29 | **Comp & gift-card counts captured at check-in (Door Attendant → FS)** — relocate/formalize the capture flow. | Phase 3 roles/use-case review 2026-07-12 (Door Attendant/FS) | Phase 3 (roles + UI) / `/checkin` + `door_records` | At check-in the **Door Attendant** records whether each attendee is **comp'd** ("next dance free" card or a performer "plus one") or **redeems a gift card**. These roll up into `door_records.comp_count` and `door_records.gift_card_redemption_count`, which are **materialized on the door record**; when the **FS** opens the door record they see the counts and **confirm them with the Door Attendant**. **Counts only — never who** was comp'd/redeemed; likely just per-event counters, **not** flags on the `attendance` line (attendance rows purge at 90 days and carry no such flag today; counters on the door record persist). **Role boundary:** the Door Attendant works in `/checkin` and does **not** access `/gate`. Revises feature **014**, which currently captures `comp_count` at `/gate`; capture should move to `/checkin` with `/gate` showing the materialized figure for FS confirmation. **Resolves B21** (gift-card count gets its capture point here). |
| B28 | **Performer payment override — payments distinct from bookings** — FS pays per booked rate but may override payee and amount. | Phase 3 roles/use-case review 2026-07-12 (FS) | Phase 3 (roles + UI) / `bookings` / new payments model | The FS writes checks to pay performers per the rates on the booking but **may override payment instructions**, in two shapes today's model can't express: (a) **substitution** — the payee differs from the booked performer (Jane & Eileen booked; Eileen snowed in, Audrey sits in → FS pays *Audrey*, not Eileen); (b) **redirection + aggregation** — amounts redistributed across payees and **one check covers multiple bookings** (Cindy/Arthur/Ted booked at $125 each; Ted redirects his share to Cindy → FS writes Cindy **$250** (one check), Arthur $125, Ted $0). Today `bookings` conflates booking and payment (`performer_id` + `pay_cents` + `check_number` on one row, unused `is_overridden` bool); neither shape fits. Needs a **payment** concept separable from the booking: **payee** (contact, may ≠ booked performer), **actual amount**, **check number**, override reason, and a **many-bookings-to-one-payment** link so a single check can satisfy several booking obligations. Decide: extend `bookings` with payee/amount overrides vs. a dedicated `performer_payments` table (the aggregation case favors a separate table). The booked rate stays the *default/expected* pay; the payment records what was actually disbursed. |
| B32 | **User authentication / login + session foundation (staff roles)** — the prerequisite for enforcing any of the role model. | Phase 3 roles/use-case review 2026-07-12 (auth gap) | Phase 3 (**foundation — spec first**) | There is **no authentication in the app today**: no auth library, no `middleware.ts`, no session/login/cookie handling; roles exist only as the unused `volunteer_role` enum with zero route/field gating. The entire `role × capability × scope` model in `docs/use-cases.md` assumes an authenticated identity to attach grants to, and there is nothing to attach to. This is distinct from **B2** (which is *non-volunteer* contact login + self-service profiles); B32 is the **staff/volunteer** login + session layer that must exist before the Booker/FS/Treasurer/etc. permission enforcement can be built. `/speckit-specify` on a permissions feature will *assume* auth as a dependency and not design it — so auth needs its own spec (or to be explicitly bundled as the first Phase 3 security feature). Needs: identity + credential model, login/logout, session management, and the hook the authorization layer reads. Decide: library (e.g. Auth.js/Lucia/iron-session) vs. hand-rolled; how role grants attach to a contact (extends the `volunteer_roles` array, or a new grants table for per-series scope). |

## How to use this file

- When deferring something during `/speckit-specify` or `/speckit-plan`, add a row here and a one-line
  pointer in the originating spec's Assumptions section.
- When a backlog item is picked up, move its detail into the target feature's spec and remove the row
  (or mark it Done with the feature number).
